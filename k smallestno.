#include<bits/stdc++.h.>
using namespace std;
int main(){
int n;cin>>n;
vector<int>arr(n);
for(int i=0;i<n;i++){
cin>>arr[i];}
for(int i=0;i<=n;i++){
int m=i;
for(int j=i;j<=n-1;j++){
if(arr[j]<arr[m]) m=j;}
swap(arr[i],arr[m]);}
cout<<arr[k-1];}
